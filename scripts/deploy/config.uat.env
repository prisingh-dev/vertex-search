# =============================================================================
# Agent Serving Config — UAT Environment
# =============================================================================

# GCP Project ID
PROJECT_ID=gcp-wd-shopping-agent-uat

# Retail API location (always 'global' for Retail API)
LOCATION=global

# Retail catalog ID (standard default)
CATALOG_ID=default_catalog

# Serving config resource ID
AGENT_CONFIG_ID=agent-serving-config